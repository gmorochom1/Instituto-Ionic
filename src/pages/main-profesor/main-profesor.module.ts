import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { MainProfesorPage } from './main-profesor';

@NgModule({
  declarations: [
    MainProfesorPage,
  ],
  imports: [
    IonicPageModule.forChild(MainProfesorPage),
  ],
})
export class MainProfesorPageModule {}
